import React from 'react';
import { Route } from 'react-router-dom';
import Events from '../../containers/Events'

// PROPS: props, pastEvents, futureEvents
const ProfileRoutes = props => {
  return (
    <>
    </>
  )
}

export default ProfileRoutes;
